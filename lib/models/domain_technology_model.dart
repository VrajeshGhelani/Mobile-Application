class DomainTechnology {
  final String name;
  final String
  type; // Language / Framework / Tool / Database / Platform / Library / Concept

  DomainTechnology({required this.name, required this.type});
}
